version: '1.0'
steps:
  Building:
    title: Building
    type: build
    image_name: markfirmware/torodocker
    dockerfile: markfirmware/docker/torodocker.dockerfile
  Pushing:
    title: Pushing
    type: push
    candidate: '${{Building}}'
    tag: '${{CF_BRANCH}}-${{CF_BUILD_TIMESTAMP}}'
