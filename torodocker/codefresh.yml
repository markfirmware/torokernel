version: '1.0'
steps:
  Building:
    title: Building
    type: build
    image_name: markfirmware/torodocker
    dockerfile: torodocker/torodocker.dockerfile
  PushingLatest:
    title: Pushing latest
    type: push
    candidate: '${{Building}}'
  PushingSpecific:
    title: Pushing specific
    type: push
    candidate: '${{Building}}'
    tag: '${{CF_BRANCH}}-${{CF_BUILD_TIMESTAMP}}'
