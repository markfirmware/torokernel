version: 2
executorType: docker
containerInfo:
  - image: markfirmware/torodocker:torodocker-1500629424965
stages:
  build:
    workDir: ~/ToroKernel
    steps:
      - checkout
      - run: cd markfirmware && pwd && ls && ./linux-testelf.sh
      - store_artifacts:
          path: artifacts
          destination: testelf
